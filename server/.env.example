# MongoDB connection string (same as your Streamlit app)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGO_DB=rental_rooms

# Simple app password (same as Streamlit APP_PASSWORD)
APP_PASSWORD=choose-a-strong-password

# Server port
PORT=5000
